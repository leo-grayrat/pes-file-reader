# IDE / 工具生成的目录，不入库
.arts/
.codeartsdoer/
__pycache__/
*.pyc

# Python 虚拟环境
.venv/
venv/

# 解密后的中间产物（明文存档数据，体积大，不入库）
decoded/

# 原始存档样例（体积大，不入库）
examples/

# 游戏本体与第三方 dll（大二进制，不入库）
game/

# 社区工具资源（含可执行文件与大型附件，不入库）
resources/

# 工具/IDE 生成的快照文件
.merkle-snapshot.json

# .NET CLI 缓存（dotnet tool/首次体验产生的文件，不入库）
.dotnet-home/

# 第三方逆向工具与临时反编译项目（二进制体积大，不入库）
tools/

# WorkBuddy 项目数据（会话输出、 记忆日志，属本地工作区状态，不入库）
.workbuddy/

# 提取出的原始游戏数据包（RAW bin，体积大，不入库）
outputs/trace_seg.bin
outputs/cpk_extract_dt10/
outputs/cpk_extract_dt11/
outputs/cpk_extract/

# 脚本生成的超大产物（可由脚本随时重建，不入库）
outputs/player_browser.html
outputs/edit_player_abilities.csv