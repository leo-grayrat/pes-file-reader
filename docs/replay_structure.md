# PES2021 回放（REPLAY）data 块结构分析

> 分析对象：`examples/rep/REPLAY00000000~00000031`（50 个，十六进制编号）经
> `pes_decrypt.py` 解密后的 data 块，存放于 `decoded/rep_REPLAY*.data`。
> 分析脚本：根目录 `replay_analyze.py`（子命令：`entropy` / `strings` / `header` /
> `consensus` / `period` / `tail` / `records` / `fields` / `boundary` / `headseg` /
> `frames` / `events`，默认 `all`）。
> 本文档如实记录当前进展，分【已确认】【疑似】【未解】三档。
> 本轮（任务 #11）结合 `docs/exe_analysis.md` 的 exe 静态分析锚点（回放写出例程
> 0x01FDC500：`0x3AA0 + 0x51B1C0 = 0x51EC60` 拆分）完成了分段边界验证，
> 并**破解了事件流的帧网格结构**。

## 样本统计

| 项目 | 数值 |
|------|------|
| 样本数 | 50 个回放（编号 00~31，十六进制） |
| 原始文件大小 | 均为 5,383,087 字节（加密后） |
| data 块大小 | 均为 5,368,928 字节（0x51EC60），50/50 一致 |
| 外层布局 | 320(加密头)+208(文件头)+384(desc)+13157(logo)+5368928(data)+90(serial)，合计 5,383,087，零误差 |
| serial 区 | 占字节数 = 头字段 `serialLength`(45) × 2（按字符计长，已校正） |
| description | 仅含回放序号 01~50，无比赛元数据 |
| serial | 50 个样本完全相同（同一主机的序列号） |
| data 整体熵 | 约 6.15（非加密态；32.1% 为 0x00 字节，无 ≥4KB 的连续同值填充段） |
| **二级分段** | **0x3AA0(15,008 名单/头部段) + 0x51B1C0(5,353,920 事件流) = 0x51EC60，50/50 闭合** |

## 一、已确认（多样本验证、可直接用于解析）

### 1.1 data 块头部（0x00~0x7F，与 BL/ML 的 data 头同构）

| 偏移 | 值（50 样本） | 含义 |
|------|---------------|------|
| `+0x00` | `13`（恒定） | 类型标识 = REPLAY（BL=11、ML=10，与交接文档一致） |
| `+0x04` | `80`（恒定） | 头部自身大小 0x50（BL/ML 亦为 80） |
| `+0x08` | `5368848` = 0x51EC10（恒定） | **自洽字段：恒等于 data 块大小 − 0x50**，即负载长度 |
| `+0x0C` | 10401/10500/10600/10700/10702，随比赛变化 | 与 `+0x10` 强相关（见 1.2） |
| `+0x10`/`+0x14` | 400/500/600/700（两者恒相等），随比赛变化 | 疑似名单记录容量/参赛球员规模计数 |
| `+0x20` | `48`（恒定） | 固定，含义未知 |
| `+0x30~0x50` | 32 字节，样本间全变 | 哈希/校验区（与 BL/ML 同位置行为一致） |
| `+0x54` | `15008` = 0x3AA0（**50/50 恒定**） | **名单/头部段长度**（本轮新解，见 1.4） |
| `+0x58` | `8112`（**50/50 恒定**） | **事件流帧大小**（本轮新解，见 1.5） |
| `+0x60` | `660`（**50/50 恒定**） | **事件流帧数**（本轮新解，见 1.5） |

### 1.2 `+0x0C` 与 `+0x10` 的相关性（50 样本全量）

```
(+0x0C, +0x10) 组合：
  (10401, 400) × 3    (10500, 500) × 4    (10600, 600) × 2
  (10700, 700) × 40   (10702, 700) × 1
```

一一对应、无例外。规律形如 `+0x0C ≈ 10000 + +0x10`（存在 10401、10702 两个微偏差，
可能是编码细节而非噪声）。语义未定，但作为配对字段可信。

### 1.3 名单记录数组（0x80 起，步长 0xA0，名字在记录内 +0x58）

- 每条记录 **0xA0 (160)** 字节；名字为 9 字节大写 ASCII，位于记录内 `+0x58`。
- 记录前 2 个 u16 构成"魔数"，标识所属阵营：
  - 样本0：`0x2CBDFFFD`×11（皇家社会首发：A. REMIRO / LE NORMAND / ZUBELDIA /
    GOROSABEL / MONREAL / ILLARRA / SILVA / MERINO / PORTU / OYARZABAL / ISAK），
    `0x247AFFFE`×11（对手首发）。
  - 样本1：`0x254EFFFD`×11 + `0x17BBFFFE`×11（完全不同的两套阵容）。
  - 低 16 位疑似队伍 ID，最低字节 `FD`=主队 / `FE`=客队（推测，见二档）。
- 记录 `+0x04` 处为 4 字节整数，多数为 5~6 位（如 106956、122111），疑为**球员 ID**。
  个别记录该字段呈现为浮点值（如 `0x433C0000`≈188.0），疑为身高，字段布局尚未精确切分。
- 第 22~24 条（`0x80+0xA0*22~24`）魔数 `0x0000FFFF`，名字 `REFEREEA/B/C`，
  `+0x04` 为 1/19/16 之类小整数（裁判编号/风格）。
- 其后为若干全零的 `0x0000FFFF` 占位记录；`0x80+0xA0*39` 处出现一次 `0x00402236`
  （数组收尾标志，样本间取值不同）。
- 两样本同位名字对比：样本0 vs 样本1 仅 18/40 同位相同 → **名单随比赛变化，非固定模板**。
- **全名已解（本轮新确认，见疑似 1 修正）**：名单段内 [0x2800, 0x2E00) 为
  **明文全名表**，22 个全名按序与前 22 条首发记录一一对应（50/50 样本均成立，
  `decode_dump.py` 全量提取）；带连字符长名（如 MILINKOVIĆ-SAVIĆ）因含非 ASCII 字符
  被切成两段，需把以 `-` 开头的片段并回前一条。
- 本轮补充：样本0 在 [0x80, 0x3AA0) 按 0xA0 网格共 93 个记录位，其中 **72 个非零**
  （首发 11+11 + 裁判 3 + 替补/其他实体若干 + 记录内 `3F800000`(float 1.0) 等模板值）。

### 1.4 二级分段：名单/头部段 0x3AA0 + 事件流 0x51B1C0（50/50 闭合）

- exe 写出例程（`docs/exe_analysis.md` ①，文件偏移 0x01FDC500~0x01FDC600）给出
  `0x3AA0(15,008) + 0x51B1C0(5,353,920) = 0x51EC60` 拆分；数据侧验证：
  - 50/50 样本 `len(data) == 0x3AA0 + 0x51B1C0`；
  - **头部 `+0x54` 恒等于 0x3AA0（50/50）**——头部自带该分段长度，自洽闭环；
  - [0, 0x3AA0) 内最后非零字节：样本间 0x3657~0x38F6，均在段内，段尾为零填充；
  - 0x3AA0 处存在清晰结构突变：以 0x100 窗、0x40 步长滑动，熵从 0.0（纯零区）
    经 0.53→2.55→6.29 快速爬升，跨样本差异率同步从 0% 跳至 ~80%。
- 名单/头部段内部布局：0x80~(约)0x1980 记录数组，**[0x2800, 0x2E00) 为明文全名表**
  （22 条，与首发记录一一对应，本轮新解，见疑似 1 修正），段内其余长字符串区混有
  伪字符串，0x3980 之后纯零填充至段尾。

### 1.5 事件流帧网格：660 帧 × 8112 字节（50/50 闭合）

- **头部 `+0x58`(8112) × `+0x60`(660) = 5,353,920 = 0x51B1C0，50/50 精确闭合**——
  事件流不是无结构的变长流，而是**定长帧数组**。
- 每帧 8112 字节，内部布局（样本0 全 660 帧验证）：

| 帧内偏移 | 长度 | 内容 |
|----------|------|------|
| `+0x00~0x0F` | 16B | 帧头。字节 [1:3] 为 **u16 比赛时钟**（单调递增，相邻步长 5 或 10，全场恰好回绕一次；样本0 为 +10×289 / +5×368，疑与回放倍速/暂停有关）；字节 4~15 为小整数状态字段（如 `05 00 13 13 40 00 02 00 04 3A 36 00`，随比赛阶段微变） |
| `+0x10~0xFFF` | 4096B | **状态表：256 条 × 16B**。样本0 帧0 有 229 条非零；条目含成对 u16 字段与偶发的 `3F800000`(float 1.0)；帧间同位相同率约 52%（逐帧变化的实体状态快照） |
| `+0x1010~0x1FB0` | 4000B | **事件区**，见 1.6 |

- 事件流首 4 字节（帧0 时钟高 16 位 + 低 16 位）跨样本各不相同（如样本0 =
  `00 17 7F 47`），随录制时刻变化，与"比赛内时钟"一致，非全局墙钟。

### 1.6 事件区：16B 子头 + 10 个球员槽包（槽间距恒 300 字节）

- 事件区头 16B：`u16@+0`（191~204 间小整数，帧间波动）、`u16@+2`（**全场单调递增**，
  样本0 为 92→1388，疑为比赛进度计数）、`u16@+4`（{685, 1388} 二值，疑为阶段上限：
  上半场计数到 685 后进入下半场直至 1388）、`+8~+16` 恒为 `01 00 00…`（660/660）。
- 槽标记 `00 00 01 XX`（2 ≤ XX ≤ 40）：样本0 每帧稳定 **10 个槽，槽号 12~21**
  （帧 0~659 抽样全部一致；个别帧 11 个，多出槽 11；跨样本结构相同）。
- **槽间距恒 300 字节**（帧0 全 9 个间距均为 300）。
- 槽内结构：12B 头（`01 XX 00…`，个别槽头 +6 处带 `98 4C 4C` 等标记）+
  **20×i16 小整数序列**（逐帧变化，样本0 槽12 帧0/1/2 仅 4/20 相同，疑为姿态/动画码）+
  约 248B **高熵 blob**（熵接近随机，疑为压缩或加密的运动轨迹数据）。
- 已按上述规则切出前 100 个槽包并逐条给出候选解读（`python replay_analyze.py events`）。

### 1.7 全局长度公式

```
原始文件 = 320 + 208 + 384 + 13157 + 5368928 + 45×2 = 5383087 ✓（50/50）
data 内：+0x08 = len(data) − 0x50 ✓（50/50）
data 内：+0x54 = 0x3AA0，+0x58 × +0x60 = 0x51B1C0，两者之和 = len(data) ✓（50/50）
```

## 二、疑似（有证据支撑但未闭环验证）

1. **名单段中的长字符串表（约 0x1D00~0x3600）——本轮重大修正**：其中 [0x2800, 0x2E00)
   子区为**完整明文全名表**（22 个全名，如 `NACHO MONREAL`、`MIKEL OYARZABAL`），
   与记录数组前 22 条首发按序一一对应（50/50 样本闭合），此前“疑扰码、不建议当明文处理”
   的结论**作废**；带连字符且含非 ASCII 字符的长名会被切成两段，解析时需把以 `-` 开头
   的片段并回前一条。该区间其余部分仍混有小写字母伪字符串（`wSwzw`、`uyvgw`…），
   性质待查。
2. **队名出现点**：样本0 中 `REAL`（来自 REAL SOCIEDAD）命中 3 处
   （0x35B、0x2905、0x324A），均落在长字符串表范围内 → 该表兼含队名。
3. **帧头字节0**：多数帧为 0，样本0 帧24 起变 0x80 等；疑为时钟高位/标志位，
   与时钟步长切换（+10 → +5）同步出现，未闭环。
4. **状态表（256×16B）条目语义**：条目内成对 u16 值域集中（样本0 帧0 首位
   (0,0)×52、(0,0xFF)×6 等），含浮点模板；疑为每实体（球员+球+裁判+镜头）的
   逐帧状态，具体列语义未定。
5. **事件区 `u16@+2` = 比赛进度**：单调递增 92→1388，`u16@+4` 在 685 处换档，
   与"上下半场"叙事吻合，但未与真实比赛分钟数标定。
6. **槽号 12~21 的实体映射**：恰 10 个，疑为"持球相关球员 + 球"或关键实体子集
   （22 名球员中每帧只写 10 个），槽 16 头部额外带 `98 4C 4C` 标记（疑为守门员/球）。
7. **头部 `+0x68~+0x7C` 小整数**（样本0：20/1/9/2/255/25；样本1：10/…/10/2/255/25），
   疑为阵型人数、换人数、难度等比赛参数。
8. **尾部固定尾标（0x51EC10~0x51EC60）**：`[0x51EC10, 0x51EC3A)` 恒为 `00…`；
   `+0x3A` 处恒为 `08 18`（疑似结束魔数）；其后 12 个 i16（样本间变化，
   形如 `F3 00 B4 FF EE FF …`，位于最后一帧事件区内），最后 9 字节 0 填充。
   恰好 `+0x08`(0x51EC10) 指向该零填充段起点 → "负载长度"自洽闭环。

## 三、未解

1. **槽内 248B blob 的量化坐标语义（已部分解码，见 §七）**：blob 非加密、非 float32，
   而是「72×u16 计数器头 + 24×u16 结构化区（含量化坐标）+ 4B 枚举 + 52B 零尾」。
   剩余壁垒是结构化区各 u16 字段的**精确语义与量化比例尺**（uint16→场内坐标的轴/比例），
   需对照游戏内存或反汇编写出例程才能标定。这是事件流语义还原的最后一步。
2. 20×i16 姿态/动画码的字典（数值逐帧抖动，疑含动作编号+朝向+混合权重）。
3. 状态表（256×16B）各列语义。
4. `+0x54`=15008 已解，但 `+0x64`（每样本不同，1922~36481）语义仍未知；
   疑为随机种子或事件总数。
5. 名单记录内浮点字段的确切布局（哪个是身高/坐标/能力值）。
6. 长字符串表中全名表之外的伪字符串部分（若有扰码，方式未定）与完整字段表。
7. `+0x30~0x50` 哈希算法（修改器实操时需确认游戏是否校验）。
8. 球员 ID 与事件流的关联：名单 +0x04 的球员 ID 在事件流按 u32 检索零命中
   （随机期望亦≈0），事件流不直接引用球员全库 ID，改用槽号（12~21）等局部索引。

## 四、跨样本共识分析说明

- 5 样本按位 AND/OR 共识：**28.32% 字节恒定**；恒定 run 多为名单记录内的浮点模板
  （如 `[0xE1,0x115)`、`[0x183,0x1C0)` 等，长度 51~83 字节，周期 0xA0），
  说明名单记录骨架（能力浮点默认值）跨比赛稳定，变的是名字/ID/首发选择。
- 名单/头部段 [0, 0x3AA0) 5 样本恒定占比 72.07%（大量零填充拉高）。
- 变化最集中的 64KB 桶分散在 0x90000、0xB0000、0x1B0000~0x1E0000、0x2E0000~0x2F0000，
  即事件流内部无单一热点，符合"整段都是逐帧比赛数据"的判断。
- 帧0 vs 帧1：状态表相同 51.8%、事件区相同 51.0% → 帧间约一半字节为模板/重复结构。

## 五、下一步建议

1. 攻槽内高熵 blob：对同槽跨帧序列做差分/异或分析（若为流式加密，同槽相邻帧
   异或应暴露明文相关性）；或在游戏中录制已知轨迹（球员原地不动）做对照样本。
2. 用 22~24 条裁判记录与尾部 12 个 i16 做修改器最小实验（改值→重加密→进游戏
   验证是否被读取/校验）。
3. 对照 `docs/exe_analysis.md` 的块大小分派函数（0x014092D0），把 BL/ML/EDIT
   各类型样本的 `dataSize` 与 16 个 SIZE 逐一匹配，建立类型编号字典。
4. 若可获取游戏内存或反汇编写出例程后续代码，直接读取帧/槽结构的字段定义。

## 六、blob 编码检测结论（2026-08-28，replay_zlib.py）

对 248B 高熵 blob（全 660 帧 6614 个）做编码探测，**排除标准压缩，并发现明文结构**：

1. **非 zlib/gzip/deflate**：blob 首字节分布均匀（0x78 仅 22/6614），
   `zlib.decompress`（zlib / raw-deflate / gzip 三种 wbits）前 40 个 blob **全部失败**。
2. **非强加密**：blob 首 4 字节存在**明文递增计数器**——槽12 跨 660 帧，第 2 字节 b1
   单调递增（相邻步长 0×432 / +1×105 / -1×105），第 4 字节 b3 先增后减；
   强加密不可能保留这种明文单调模式。
3. **相邻帧强相关**：同槽跨帧 blob 异或熵 3.87，显著低于本体熵 5.6（本体熵 5.35~5.94）。
4. **推断**：blob 大概率是「明文计数/时间戳头 + 运动数据（经轻度线性变换或 delta 编码，
   非标准压缩、非强加密）」。下一步应沿"剥掉头字段后对余下 240B 做同槽跨帧差分/异或"，
   观察是否有 float32 坐标/速度的低字节相关性；或反汇编写出例程 0x01FDC500 后续逻辑。

## 七、blob 内部布局（2026-08-30，probe_replay_blob*.py）

对**槽12 跨 660 帧**的 248B blob 做逐字节跨帧 XOR 熵 + uint16 重解 + hex 直视
（`probe_replay_blob.py` 修 `bi[2]→bi[1]` 崩溃后跑通，`probe_replay_blob2/3/4.py` 递进）。
**重要更正：§6.4 的 float32 坐标假设被证伪**——blob 内不含任何 float32，运动数据以
**uint16 量化（0–65535）**存储。

### 7.1 四区布局

| 区 | 偏移 | 长度 | 内容 |
|----|------|------|------|
| 头部 | `[0:144]` | 144B = 72×u16 | 每个 u16 的**高字节逐帧缓增（+1~+4/帧，计数/时间戳）**、低字节快速回绕（单帧内高熵，看似随机）。例 u16[0] 跨帧 59268→59432→59550→59667（步长 ~120–164，低字节回绕）。首字节 b1 单调不减占比 83%。→ 事件元数据/时间戳/ID 数组 |
| 结构化数据 | `[144:192]` | 48B = 24×u16 | 见 7.2 |
| 枚举字段 | `[192:196]` | 4B | 逐帧取多个 u32 值（0x00040111=262417 占 275 帧、0x00010155=65877 占 203 帧、其余零星），**非恒定**，疑为阶段/事件枚举 |
| 零填充 | `[196:248]` | 52B | 全零（660/660） |

### 7.2 结构化数据 `[144:192]`（24×u16）逐帧轨迹（帧0,1,2,3,100,659）

```
帧0:   10416   0   302   0  8847   5   0 61492 11121  29 63557  191  537 2296  537  203  191 2296  537  203  191 2296  537  203
帧1:   10419   0   306   0  8853   5   0 61497 11154  27 63589  191  555 2296  555  203  191 2296  555  203  191 2296  555  203
帧100: 10424   0   280   0  8306 65527 0 62351 11970 30 65414  201  277  200  277  685  201  200  277  685  201  200  277  685
帧659: 10412   0   298   0  8874   5   0 61487 11088  31 63525  191  518 2296  518  203  191 2296  518  203  191 2296  518  203
```

- **前导计数器/ID**：`+144` 帧时钟（+2~3/帧）、`+148`（+3/帧）、`+152/+154`（u32 变化）、
  `+158/+160`（缓变）、`+162`（小整数 27–1600）、`+164`（大整数 0–65534 变化）。
- **`+156`(u16#6) ≡ 0 恒成立**（全局常量占位/标志字段，全 (帧,槽) 点主峰 0 占 100%）。
- **尾部标记是双峰状态值，非坐标**（2026-08-31 十槽复核，见 §7.5）：`191 / 2296 / 203`
  所在的偏移（u16#11/#13/#16/#17/#20/#21）呈双峰分布（主峰≈191 占 17–25%、次峰≈14–18%），
  是「8 默认槽 + 2 特殊槽」的**状态/ID 标志**，不是连续坐标。早先「疑似量化坐标」的猜测作废。

### 7.3 编码结论（修正 §6 与早先 float32 猜测）

- blob **非加密**：头部 72 个 u16 计数器、结构化区 u16 时间戳、零尾均明文可见。
- blob **非 float32**：float32(LE) 重解全为 denormal（≈0），系 uint16 低字被误读；坐标为
  uint16 量化（或状态量）。
- 跨帧 XOR 熵 3.742（与 §6 的 3.87 一致）证实数值 delta/量化编码、逐帧强相关。
- **blob 内不存在可直接读取的 (x,y) 原始坐标对**（§7.5 坐标标定尝试为负结果）：
  结构化区 `[144:192]` 是「连续计数器/量 + 双峰状态标志 + 冗余别名偏移 + 常量0」的混合，
  坐标由游戏运行时从 20×i16 姿态/动画码（§1.6）重建，**不在 blob 里**。故无法仅靠「标定
  uint16→坐标比例尺」还原位置——须走姿态码/运行时映射。

### 7.4 开放项

1. 头部 72 个 u16 计数器的确切含义（是否含 RNG 种子/校验；首 u16 在帧100 出现非单调，
   疑因槽选取跨帧混入不同事件实例，需按事件身份稳定化后再做逐帧轨迹）。
2. 结构化区 `[144:192]` 各字段语义：连续偏移（u16#0/#2/#4/#5/#7/#8/#12/#14/#18/#22/#23）
   哪些是计数器、哪些是实体状态量；`+156` 零字段用途；冗余别名（u16#12=#14=#18=#22 值全同）。
3. 枚举字段 `[192:196]` 的取值枚举表与语义。
4. **坐标还原的真正路径**：20×i16 姿态/动画码（§1.6）的字典 + 游戏运行时映射，而非 blob 内缩放。

### 7.5 坐标标定尝试（2026-08-31，probe_replay_blob5.py，负结果）

把分析从「槽12 单槽」扩到 **10 槽(12~21) × 660 帧**，关键修正早先 slot-12-only 的偏差：

- **全部 24 个 u16 偏移都随槽变化**（无全局常量字段）；早先 slot-12 看成的「+156 恒0 /
  191/2296/203 常量模板」只是槽12 自身的值，跨槽即变 → 证实结构化区是**逐实体**的。
- 逐偏移离散度分三类：① **连续量**（u16#0/#2/#4/#5/#7/#8/#12/#14/#18/#22/#23，主峰占比 0–7%，
  满量程铺开）；② **双峰状态标志**（u16#11/#13/#16/#17/#20/#21，主峰≈191 占 17–25% + 次峰
  14–18%，即「8 默认 + 2 特殊」槽的标记）；③ **常量0**（u16#6=+156）。
- 用「球场长宽比 105:68≈1.544 + 两轴各向同性」找 (x,y) 坐标对：命中 20 对，但逐帧 10 槽散点
  **无一形成真实阵型**——候选「y」偏移全是双峰（8 槽≈200/~0.6m，2 槽≈2300/~7m）；退化对
  (u16#13,u16#21) 每槽 x≈y；frame0 时 x 全 0 的对亦存在。aspect 匹配纯属量程巧合。
- **唯一两轴皆铺开的对 (u16#4×u16#5)**：frame0 散点 x∈[4708,23347]、y∈[5,10995]；若按
  x 满量程对 105m（scale≈222 uint16/m）换算点云落在半场 [21,105]×[0,49]m，形似阵型但
  **未确认**（aspect 1.70≠1.544，亦可能是两个无关连续量）。列为弱线索，非结论。
- **结论**：blob 结构化区无干净原始 (x,y) 坐标场；坐标由运行时从姿态码重建。与 2026-08-30
  「回放是状态/动画流、非位置流」结论一致。标定比例尺之路在 blob 内走不通。

## 八、exe 静态分析交叉验证（2026-08-30，官方 Patch 1.07.00 exe）

> 目标：`resources/Patch 1.07.00/eFootball PES 2021/PES2021.exe`（468,030,464 B）。
> 此前 `docs/exe_analysis.md` 的回放锚点按 1.01.00 定位在 `0x01FDC500`，本构建偏移漂移；
> 实跑字节模式搜索确认本构建回放写出例程在 `0x1FE0E20` 一带（`mov edx,0x51EC60` 实际命中
> `0x1FE12C9`）。capstone 5.0.9 在隔离 venv。分析方法：**纯只读 mmap 反汇编**，不改写、不执行。
> 脚本：`exe_dis_func.py` / `exe_find_frame_ctrs.py`（搜 8112/660/300/248/20 立即数）/
> `exe_find_slot_writer.py`（修正 0x12C 步长判定，排除 `C6 81` 脏标记假阳性）/
> `exe_dump_vtable.py`（vtable 路线，本构建未命中，弃用）。

### 8.1 数据侧 ↔ exe 静态 双向闭环（全部坐实）

| 数据侧已确认结论 | exe 内静态证据（文件偏移） | 判定 |
|---|---|---|
| 回放 data = 0x51EC60 = 0x3AA0 + 0x51B1C0（§1.4） | 保存函数 `0x1FE0E20`：`mov edx,0x51ec60`→`call` 分配缓冲(type 26)；`mov r9d,0x3aa0` 拷名单；`mov edx,0x51b1c0`→`call 0xafa0d0` 拷事件流（事件流由 `call [rdx+0x40]` vtable 虚方法填充，保存到时整段拷入） | **交叉验证成立** |
| 名单步长 0xA0（§1.3） | 保存函数双循环：源记录 `r14=[r15+0x80]+esi*0xa0`；`cmp esi,0x27`（39 次）；工作数组步长 `0x17c`(380) | **交叉验证成立** |
| 球员对象 = 0x17c(380) = CT ptrPlayer（§1.3 脚注） | 同上：`imul rdi,rax,0x17c` 定位每个球员对象；与 CT ptrPlayer 字段字典一致 | **交叉验证成立** |
| 每帧 8112 字节（§1.5） | ReplayFrame 构造函数 `0x00A89C10`：`mov edx,0x1fb0`(8112)→`call 0x3fa0c0` 分配**单帧缓冲**（type 26=REPLAY） | **交叉验证成立** —— 8112 全 exe 仅 2 处立即数命中，特异性极强 |
| **槽间距 300（§1.6）** | **ReplayFrame 类槽方法群 `0x00A8Dxxx`：`imul ...,0x12c`(300) 为槽步长；`cmp edx,0x19`(25) 为容量上限**；getter 读各槽 `0x374`(打包 3+3+2 位域)/`0x375`(字节)/`0x382`(字)；数组构造器 `0x00A8D900` 以 0x12c 步长、25 次循环初始化（0x7FFF 哨兵） | **交叉验证成立（关键新证）** |
| 槽内 248B blob（§7） | 槽/实体构造器 `0x00A8D8A0`：`mov r8d,0x12c`(300) 分配整槽；槽内布局 12B 头 + 20 项 + 248B blob（12+40+248=300 自洽）；邻域含 `0xF8`(248) 立即数 | **一致** |
| 20×i16 姿态码（§1.6） | 槽构造器 `0x00A8D8A0`：在 [+4] 处 `mov edx,0x14`(20) 循环初始化 **20 个 float32**（值 0x28be0000）+ 0x3f800000(1.0f) 哨兵 | **类型分歧已解决（见 §8.5）**：数据侧 20×i16 是序列化量化形式（落在槽 [12:52)），静态 `0x00A8D8A0` 的 20×(i32+f32) 是内存工作副本（落在 [4:164)）；二者为同一逻辑字段（姿态/动画码）的两种表示，非矛盾。**300B 尺寸双向坐实，但内存槽与序列化槽内部字段顺序不同**（见 §8.5 修正）。 |

### 8.2 回放保存函数 `0x1FE0E20` 行为（节选）

```
0x1FE0E20 函数 prologue
... 构建 39 个球员对象的工作数组(步长 0x17c)，源名单步长 0xa0 ...
0x1FE12C9: mov edx, 0x51ec60      ; 回放总缓冲 5,368,928
0x1FE12CE: call 0x3fa0c0         ; 分配缓冲(type 26=REPLAY)
0x1FE12DA: mov r9d, 0x3aa0       ; 名单/头部段 15,008
0x1FE12ED: call 0xafa0d0         ; 拷 0x3aa0 字节入 buffer[0]
0x1FE12FE: mov edx, 0x51b1c0     ; 事件流 5,353,920
0x1FE1306: call 0xafa0d0         ; 拷 0x51b1c0 字节入 buffer[0x3aa0]
```
→ 帧网格 / 槽 / blob 的**逐字节填充不在保存函数里**，而由比赛实录引擎经 `call [rdx+0x40]`
（vtable 虚方法）在录制期写好 0x51b1c0 缓冲，保存时整段拷入。故帧内布局须从 ReplayFrame
类（`0x00A8Dxxx` 槽方法群）与实录引擎还原，而非保存函数。

### 8.3 待复核（本论新引入的小开放项）

1. **槽内容 20 项的类型** —— **已解决（§8.5）**：数据侧 20×i16 与静态 `0x00A8D8A0` 的
   20×(i32+f32) 是同一逻辑字段（姿态/动画码）的序列化量化形式 vs 内存工作副本，非矛盾。
   详见 §8.5：序列化槽 [12:52)=20×i16；内存槽 [4:164)=20 个 (i32,f32) 记录。
2. **每帧事件区 4000B 的内部账目** —— **已精化（§8.5）**：实测槽起点在事件区内
   +360（非紧接 16B 子头），故账目为 `16B 子头 + 344B 未标前部 + 3000B(10×300 槽)
   + ~640B 尾部 = 4000B`。344B 前部 + ~640B 尾部 = 原估「余 ~984B」的拆解；槽步长 300 不变。
3. 事件流填充虚方法 `call [rdx+0x40]`（实录引擎）的本体尚未反汇编（vtable 路线本构建未命中，
   因 `0x1bcea5c` 处实为代码非 vtable 数据）；如需逐字节还原 660 帧循环，须沿实录引擎定位该方法。

### 8.4 本论新增脚本

- `exe_find_frame_ctrs.py`：全文件搜 8112/660/300/248/20 的 mov-立即数，锚定帧写出函数。
- `exe_find_slot_writer.py`：修正 `0x12C` 步长判定（真实 add/imul，排除 `C6 81` 脏标记），
  评分邻域 248/40/20/10，定位 ReplayFrame 槽方法群。
- `exe_dump_vtable.py`：vtable 路线（本构建未命中，保留作其他方法参考）。
- `exe_dis_replay.txt` / `exe_dis_slot.txt` / `exe_dis_slot2.txt`：关键函数反汇编证据。
- `probe_slot_layout.py` / `probe_slot_layout2.py` / `probe_slot_layout3.py`：序列化槽布局实证校验脚本（读 decoded/rep_REPLAY*.data，纯只读）。
- `exe_pe_sections.py`：解析 PE 段表，证明本构建段表为保护器诱饵（Raw=0、VA 错位）。
- `exe_raw_dump.py`：对比 flat 偏移 vs 段表映射偏移、dump 真实 vtable，证实 vtable 条目是混淆桩。
- `exe_dump_vtable2.py`：vtable dump（初版 `exe_dump_vtable.py` 误把 rip 位移 0x1bcea5c 当绝对地址，已废）。
- `scan_frame_ctor.py`：反汇编 ReplayFrame 构造器 `0x00A89C10`，提取帧级字段写入（mov ptr [reg+disp], imm）与子对象构造调用、取址（lea reg,[reg+off]），按偏移排序得帧对象内存布局。
- `probe_frame_layout.py`：在 `decoded/rep_REPLAY00000000.data` 定位 256×16B 状态表、探测逐帧 stride、闭合文件大小算术（0x3AA0 + 660×8112 = 0x51EC60），修正「帧0=文件头」偏移错误。

### 8.5 序列化槽布局实证校验（2026-08-30，decoded data，修正 §8.1/§8.3）

> 方法：读 `decoded/rep_REPLAY00000000.data`（5,368,928B = 0x51EC60）。帧 = 8112B，位于
> `data[0x3AA0 + fi*8112]`；事件区位于 `frame[0x1010 : 0x1FB0]`（4000B）。在事件区内扫描
> §1.6 的槽标记 `00 00 01 XX`（X∈[2,40]），定位真实槽起点与间距。全程只读已解密文件，
> 不碰运行时内存。脚本 `probe_slot_layout*.py`。

**1. 槽起点与间距（坐实 §1.6 槽间距 300）**
- 标记命中 10 个，事件区内偏移 `360, 660, 960, …, 3060`，**相邻间距恒 300**（50/50 样本同构）。
- ⇒ 真实槽起点在事件区内 **+360**，而非 §1.6 早期「紧接 16B 子头(+16)」的假设。事件区内部账目修正为
  `16B 子头 + 344B 未标前部 + 3000B(10×300) + ~640B 尾部 = 4000B`（见 §8.3#2 精化）。

**2. 序列化槽 300B 布局（实测，覆盖并精化 §1.6「12B 头 + 20×i16 + 248B blob」）**

| 偏移 | 长度 | 内容（实测，660 帧） |
|------|------|------|
| `[0:4]` | 4B | 标记 `00 00 01 XX`：b0=3+3+2 位域；b1=实体/状态字节；b2=0x01 恒定标记；b3=**槽号** |
| `[4:12]` | 8B | 头扩展（帧0 全 0；构成 12B 头的后半） |
| `[12:52]` | 40B | **20×i16**：逐帧变化。前 ~14 项为小符号整数（姿态/动画码），后 ~6 项为大整数（14000~19000 量级，疑量化坐标/角度，弱线索，待运行时映射） |
| `[52:300]` | 248B | 高熵 blob（§7） |

- **b0 3+3+2 位域**：跨 660 帧 6600 个槽仅 **1 个非零**（0.0%）→ 该位域在本样本实质休眠/恒 0，
  静态 getter（`0x00A8D5D0/0x600/0x630` 分别取 lo3/mid3/hi2）虽能解析，但数据侧无变化可标定语义。
- **b2 = 0x01**：6591/6600 恒定（其余为特殊实体槽，见下）→ 确认「00 00 01 XX」标记的第三字节。
- **b3 = 槽号**：标准 **12~21** 各 ~659 次；偶见 35/36/40/44/51/116/124 等，对应「个别帧 11 槽」
  或球/裁判等特殊实体。⇒ 每帧 10 标准槽（号 12~21），与 §1.6 吻合。

**3. 20×i16 帧间变化（确认 §1.6「逐帧变化」）**
- 槽#12（b3=12）跨帧抽样：帧0=`[0,71,52,100,24,-124,151,4,-18,255,1,0,0,0,17406,15984,17510,4598,-16080,13826]`；
  帧100=`[...,17466,13490,18833,4848,-12942,17427]` 等。前 14 项小整数抖动明显，后 6 项大整数
  量级稳定 → 前段为姿态/动画码，后段疑量化坐标（与 §7.5「blob 内无干净 (x,y)、坐标由姿态码在
  运行时重建」不冲突：此处大值为中间量化态，仍须经运行时映射还原场内坐标）。

**4. §8.1 修正：内存槽 vs 序列化槽（同逻辑实体的两种布局）**
- 数据侧实测的序列化槽（匹配 getter 类 `0x00A8D560~0x65F`，其槽数组在父对象 **+0x374**）：
  12B 标记头 + 20×i16@`[12:52)` + 248B blob；b0/b1/b2/+0xE(u16)/+8(u16) 均能被 getter 读到
  （实测槽#12 帧0 的 +0xE = 71 = 第 2 个 i16，与 getter2 返回地址吻合）。
- 静态构造器 `0x00A8D8A0`（单槽）+ 数组构造器 `0x00A8D900`（25 次）属**另一类**：槽数组在父对象
  **+0x264**，单槽 300B，在 `[0]` 写 0(dword)、在 `[4:164)` 写 **20 个 (i32, f32) 记录**（默认
  (0, 0x28be0000≈0)）、在 `[0xa8]` 写哨兵 0x7fff、在 `[0x110]/[0x124]` 写 0。
- ⇒ 二者**共享 300B 尺寸**（交叉验证成立），但内部字段顺序不同：序列化槽把 20 项量化进 `[12:52)`
  的 20×i16 并在 `[0:4]` 写 (位域,实体,0x01,槽号) 标记；内存槽以 `[4:164)` 的 20×(i32,f32) 工作
  副本暂存，序列化时量化/打包为文件形式。§8.1「槽内 248B blob … 一致」仅对 300B 尺寸成立，
  内部字段顺序的「一致」为过度声明，特此更正。
- **槽容量**：内存 25（`cmp edx,0x19` / 数组构造 25 次循环）vs 序列化每帧 10（数据侧 10 槽号 12~21）；
  另 15 槽为替补/未上场实体预留，录制期按需填充。

**5. 状态**
- §8.3#1（20 项类型分歧）、§8.3#2（事件区账目）已解决/精化。
- 仍开放：§8.3#3（事件流填充虚方法 `call [rdx+0x40]` 实录引擎本体未反汇编，vtable 路线本构建未命中）；
  20×i16 后段大值的精确坐标比例尺（需运行时映射，非纯静态可解）。

### 8.6 受保护 exe 的静态分析边界（2026-08-30，修正 vtable 路线失败的根因）

> 续 §8.5 推进：试图沿 ReplayFrame 构造器 `0x00A89C35` 的 `lea rax,[rip+0x1bcea5c]`
> 取出 vtable、再读 `+0x40` 条目还原 `call [rdx+0x40]`（实录引擎方法）。结果揭示本构建
> 真正的分析边界，纠正此前「vtable 地址算错」的误判。

**1. flat 模型（文件偏移 = 地址）对代码有效；PE 段表是诱饵**
- `exe_pe_sections.py` 解析段表得到「所有段 `PointerToRawData=0`、VA 与 Raw 错位」——
  这是**不可能**的真实布局（保护器植入的诱饵段表）。
- 证据：按段表把 `0x00A89C10`→`0xa89210` 读出的字节（`00 66 46 89 …`）不是函数序言；
  而 **flat 直接读 `0x00A89C10`** 是标准 x64 序言 `4c 8b dc 49 89 4b 08 …` 后接 `0x1fb0`(8112)
  分配，与 §1.5/§8 完全一致。⇒ 真实代码就在 flat 文件偏移处，`exe_dis_func.py` 的口径正确，
  此前全部回放反汇编（槽步长 300、容量 25、帧 8112）结论有效。

**2. vtable 路线真正受阻 = 保护器混淆，非地址错误**
- 构造器写入对象的 vtable 指针 = `0x00A89C3C + 0x01bcea5c = 0x01C79698`（flat，正确解析
  rip 位移后）。但 dump `0x01C79698` 得到的不是指针数组，而是**代码字节**
  （如 entry+0x40 = `30 f6 c1 1f 0f 85 …` = 混淆控制流桩）。⇒ 本构建的 vtable 条目是
  **混淆分派桩（trampoline/guard）**，不是干净函数指针。
- 故 `call [rdx+0x40]`（保存函数 `0x01FE12F8`）读的是混淆桩、跳进受保护代码；还原实录引擎
  方法须**去混淆或动态分析**，二者均超出用户「暂不碰需要动态检测游戏内存」的边界。
- 也正因此，§8.5 的 getter 类（直接 `imul 0x12c` / `cmp 0x19` 读槽字段）可读，而 vtable 虚方法
  本体不可读——前者是内联/直接字段访问，后者是虚拟分派被混淆。

**3. 结论与后续建议**
- §8.3#3 在**纯静态**范畴内判定为**硬阻塞**（保护器），不再列为待复核的「可推进项」；
  若未来要还原 660 帧填充循环，唯二路径是：(a) 动态 dump 运行时 vtable 实指针；(b) 对混淆桩去混淆。
  二者均属用户已排除的动态/保护突破范畴，故冻结。
- 实用提示：本 exe 的**数据侧常量（0x51ec60 / 0x3aa0 / 0x51b1c0 / 0x1fb0 / 0x12c / 0x19）与
  flat 反汇编交叉验证已足够坐实回放结构**；vtable/虚方法级还原留给动态分析。

### 8.7 ReplayFrame 构造器（内存表示）字段地图 + 与序列化帧交叉验证（2026-08-30）

> 续 §8.6 后，沿已确认的 flat 口径反汇编 ReplayFrame 构造器 `0x00A89C10`
> （`scan_frame_ctor.py`），把 prior 摘要里「帧级字段偏移 +0x698/+0x69c=0x15/+0x6a0=0x15/
> +0x6a8/+0x6b0 及子对象 +0x6b8/+0x1c80/…」映射为语义，并跨 §1.5 的序列化帧结构做闭合。

**1. 构造器确认帧对象尺寸 = 8112B（与 §1.5 序列化帧同尺寸）**
- `0x00A89C32: mov rsi, rcx`（rsi 全程持有 `this`）→ 后续 `[rsi+0xNNN]` 写入均为**帧绝对偏移**。
- `0x00A89D30: mov edx,0x1fb0`(8112) → `call 0x3fa0c0` 分配单帧缓冲；随后 `call 0xa89ff0`
  把静态模板（地址 `0xa89ff0`）`memcpy` 进该缓冲（即默认帧初值，含 256×16B 身份初始化）。
- ⇒ **内存帧对象与序列化帧同为 8112B**，尺寸三方一致（构造器 / `+0x58` 字段 / 文件大小算术）。

**2. 内存帧对象布局（flat 反汇编 0x00A89C10，字段写入 + 子对象取址）**

| 帧内偏移 | 长度 | 构造器证据 | 语义（推断） |
|----------|------|-----------|--------------|
| `+0x00` | 8B | `mov [rcx], 0x01C79698`（混淆 vtable，§8.6 冻结） | 虚表指针 |
| `+0x08` | 1680B | `lea r13,[rcx+8]`; `call 0x15a0828(rcx=r13, edx=0x50, r8d=0x15)` | **21 元素数组 × 80B**（元素 0x50、计数 0x15=21），占 `+0x08`~`+0x698` |
| `+0x698` | 4B | `mov [rsi+0x698], 0` | 字段 A（=0） |
| `+0x69c` | 4B | `mov [rsi+0x69c], 0x15`(=21) | **数组计数 21**（与 `+0x08` 数组呼应） |
| `+0x6a0` | 4B | `mov [rsi+0x6a0], 0x15`(=21) | **数组计数 21**（冗余计数/关联计数） |
| `+0x6a8` | 8B | `mov [rsi+0x6a8], r13`(=this+8) | 指针，回指 `+0x08` 数组起点 |
| `+0x6b0` | 1B | `mov [rsi+0x6b0], r14b`(=0) | 标志字节（=0） |
| `+0x6b8` | ~5424B | `lea rcx,[rsi+0x6b8]`; `call 0xa89f00`（子构造器初始化 27 元素 × 176B 数组，含默认 `float 180.0`=0x43340000 @ 每元素 +0x08） | **大子对象**：27×176B 数组（27 实体/骨骼组？），至 `+0x1BE8` |
| `+0x1c80`~`+0x1d20` | 5×~40B | `lea r15/r14/rdi/r12/rbp,[rsi+0x1c80/0x1ca8/0x1cd0/0x1cf8/0x1d20]`，各写 byte@+8=0、[+0x10..+0x20]=0、vtable@+0，再经虚方法构造 | **5 个同类小个子对象**（各带 vtable + 3 指针），疑管理器/句柄 |
| `+0x1d48`~`+0x1fb0` | ~616B | — | 尾部（未由构造器显式字段写入，属模板 memcpy / 子对象延伸） |

- 关键：构造器把帧尺寸 8112B 拆成 **vtable + 21×80 数组 + 5 个帧字段 + 大子对象(27×176) + 5 小个子对象**，
  与 §1.5 序列化帧的「16B 头 + 4096B 状态表 + 4000B 事件区」**内部组织完全不同**——前者是带虚表/子对象的
  C++ 对象，后者是扁平数据流。二者由**反序列化例程**桥接（该例程属动态/混淆范畴，§8.6 已冻结，不在纯静态可解内）。
- 内存 21 元素数组（计数 0x15=21）与序列化事件区「10 标准槽(12~21) + 11 预留槽」的数量级呼应，
  暗示该数组即逐实体（球员/球/裁判）运行时句柄表；但内存偏移↔槽号(12~21)的精确映射需反序列化源码，纯静态不可定，**列为开放项**。

**3. 与序列化帧的闭合（decoded data 实证，probe_frame_layout.py）**
- **文件大小精确算术**：`0x3AA0(15008B 名单段) + 660 × 8112 = 15008 + 5,353,920 = 5,368,928 = 0x51EC60`，
  与 §1.1 二级分段 `0x3AA0 + 0x51B1C0` 一致。⇒ 帧从**绝对偏移 0x3AA0** 起（非文件头 0x00），先前把文件头误当帧0 的偏移已纠正。
- **逐帧周期**：槽标记 `00 00 01 XX`（X=12..21）在每帧帧内 `0x1178`(4472) 起、间距 300B，660 帧全部命中；
  末帧(659)尾 = `0x51CCB0 + 8112 = 0x51EC60` = 文件尾，余 0 ⇒ 帧数 660、stride 8112 **确凿**。
- **状态表定位**：帧内 `0x10~0xFFF`(4096B) 前 8 行 = `[0,0,0,0]`+`[0,0,0,1]`×7（身份四元数），与 §1.5/§4 一致；
  抽样帧（f0 vs f330）仅 50/256 行逐帧相同 ⇒ **逐帧刷新的实体状态快照**（非静态）。

**4. 订正：outputs/replay_blob_report.md 的「状态表跨帧完全不变」为误判**
- 该报告 §4 称「256×16B 状态表…跨帧完全不变」。实证（§8.7#3）显示仅**前 8 行身份初始化静态**，
  其余 248 行逐帧变化（f0 vs f330 仅 50/256 行相同）。报告当时误取同帧/区域或把前 8 行推广到全表。
- 文档 §1.5 行 100 的「帧间同位相同率约 52%（逐帧变化的实体状态快照）」才是正确的；报告须以文档为准。
- 另：`replay_blob_report.md` 把身份四元数写作 `[0,0,0,1.0]` 的四元数**排列**未明，实据为
  `[0,0,0,1]`（w 在末位）与 `[1,0,0,0]`（w 在首位）两种单位四元数，均=旋转身份。

**5. 状态**
- 已坐实：帧尺寸 8112B（构造器/字段/文件三方）、内存帧对象字段地图、序列化帧结构（§1.5）闭合、状态表动画本质。
- 仍开放（纯静态不可解，属动态/反序列化范畴，冻结）：内存 21 数组 ↔ 序列化 10+11 槽的语义映射；
  反序列化例程（660 帧填充循环）；20×i16 后段大值坐标比例尺（运行时映射）。
