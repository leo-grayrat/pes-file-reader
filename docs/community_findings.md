# 社区逆向成果归档（资源对拍记录）

> 本文档汇总从社区现成工具中提取的、经本仓库真实存档验证的结构知识。
> 原则：一切社区结论都必须与本仓库 8 个真实存档样本对拍后才能采信。

## 资源清单

| 资源 | 作者/来源 | 性质 | 存放位置 |
|---|---|---|---|
| EconomyScaler 5.0（CPY 版） | loucopouco (Sting)，Evo-Web | Sider Lua 模块，运行时挂钩经济计算，全部钩子经游戏内实测 | `resources/[CPY] EconomyScaler 5.0/` |
| PES 2021 CT 表 v21.1.0（原版英文/汉化/FL2026 三套） | Aranaktu（汉化：gxlzliqi / kokoroposemi） | Cheat Engine 表，AOB 定位运行时对象 | `resources/CT/` |
| PES2021 BAL Career Editor 1.1 | DevilCold52 | .NET 一球成名存档编辑器（混淆），自带同源加解密器 | `resources/PES2021 BAL Career Editor 1.1/` |

## 一、金额存储单位（关键结论）

**1 存储单位 = 100 欧元**（int32 存储）。
- 证据 1（EconomyScaler 运行时对拍）：解约金弹窗 €666,800 ↔ 寄存器值 6668；
  贝克汉姆年薪 22085 ↔ €2.2085M。
- 证据 2（CT 表）：年薪、市值条目均为 4 字节有符号整数，描述标注"(Euro)"，
  换算口径一致。
- 影响：此前本仓库按"万位/百万量级"做的四轮资金扫描全部作废，需按本重新检索。

## 二、俱乐部财务字段图（exe 运行时偏移，CPY/1.01.00）

俱乐部财务是一个大对象的连续字段区（两套独立工具交叉印证）：

| 字段 | 对象内偏移 | 生成时机（EconomyScaler 实测） |
|---|---|---|
| 转会预算 | +0x16ECBF4 | 每次预算注资时累加 |
| 薪资预算（+0x14 处） | +0x16ECC08 | 随阵容薪资计算 |
| 转播权收入 | +0x16ECC20 | 赛季初一次性生成 |
| 赞助收入 | +0x16ECC24 | 赛季初一次性生成 |
| 球迷会收入 | +0x16ECC28 | 赛季初一次性生成 |
| 商品销售累计 | +0x16EF074（全局） | 赛季中实时累加 |
| 门票收入 | 赛事对象 +0x10 增量 | 赛季中实时累加 |
| 赛事奖金 | 赛事对象 +0x04/+0x08 | 赛事结束时入账 |

注意：**预算报表（Budget Report）与账户余额（Account Balance）是两套独立系统**，
字段成对出现（如门票↔门票奖励），改一侧不会自动同步另一侧。

## 三、球员记录布局（CT 运行时版，总长 0x17C=380 字节）

| 字段 | 偏移 | 类型 |
|---|---|---|
| 球员 ID | +0x000 | u32 |
| 所属队 | +0x004 | u16 |
| 所属联赛 | +0x006 | u16 |
| 解说 ID | +0x008 | u32 |
| 球员名 / 国家队球衣名 / 俱乐部球衣名 | +0x00C 起 | 字符串 |
| 国籍 | 随后 | u16 |
| 体重/身高/年龄/惯用脚 | 随后 | 压缩位段 |
| 注册位置 + 11 个可踢位置 | — | 位段 |
| 26 项能力值 + 技巧 + 风格 + 球队角色 | — | 位段 |
| **年薪** | **+0x15C** | **u32（单位 €100）** |
| 合同终止 年/月/日 | +0x160/+0x162/+0x163 | u16/u8/u8 |
| **市值** | **+0x174** | **u32（单位 €100）** |

## 四、与本仓库真实存档的对拍结果（BL00000000）

用户球员记录基址 0xB7E2C4 验证：
- +0 与 +4 均为球员 ID 143939（存档序列化为双份，与运行时布局不同）；
- +0x08 起为 UTF-8 中文名「达里奥·埃苏戈」；
- +0x44、+0x82 为 ASCII 名 DARIO ESSUGO 两份；
- +0x100 = 2023-01-31、+0x134 = 2021-01-04（合同相关日期，与此前"年份字段"发现吻合）；
- +0x170 = 2021。

**重要结论：存档是序列化格式，字段布局与运行时内存布局不一致**
（例：存档 +0x15C 是日期区而非年薪）。社区偏移不能直接照搬到存档，
但字段集合、取值语义、金额单位全部可用。

## 五、BAL Career Editor 勘察

- .NET 4.0 程序，经混淆（用户字符串堆为空、类型名全部乱码）；
- 功能截图确认：编辑用户球员的姓名、身高体重年龄、26 项能力、位置、技巧、风格——
  即对第四节验证过的同一条用户球员记录做写入；
- 自带 `decrypter21.exe/encrypter21.exe`，与本仓库 pesXdecrypter 同源；
- 其存档偏移常量藏在混淆后的 IL 中，静态提取成本高，暂不作为首选路线。
- **2026-08-28 复勘（第一轮）**：确认为 **.NET Reactor 强混淆**——元数据根内 `#Strings/#Blob/#GUID`
  流名重复、流 offset/size 被篡改（`stream is too small: wanted 0xX found 0x4`）；Python 库 `dnfile`
  读取失败（当时误记为 "dnfile/dnlib"）。
- **2026-08-28 复勘（第二轮，dnread 工具实测）**：用 dnSpy 自带的 **dnlib.dll**（C# 实现，能力远强于
   Python 的 dnfile）成功读取 IL：**15 类型 / 695 方法 / 691 个方法体 / 124 个 `ldc.i4` 立即数**。
  但结论仍是**放弃**，证据如下：
  1. 124 个 `ldc.i4` **全是加密大常量**（`0xCE6244CD`、`0x8171F705`…，无一个明文字段偏移）；
  2. IL 为 **Reactor 控制流扁平化 + 算术混淆**：`<Module>` 内一个状态机方法以
     `ldc.i4; ldc.i4; xor; rem; switch(55)` 动态分派，常量靠 xor/mul/shr/shl 链在运行时还原；
  3. 小立即数（`ldc.i4.s`）仅普通值（0x00~0x54，多为循环计数/索引），**未见 EDIT 式字段偏移**
     （0x36/0x73/0xB0 全零命中）——BAL 编辑的是 BL 存档用户球员，布局本就异于 EDIT 文档；
  4. 结论：**存档偏移是运行时动态算出的，静态提取不可行**；需 dnSpy 动态调试（GUI，agent 无法操作）
     或 de4dot 深度脱壳。收益仅为「BL 用户球员字段图」（语义已从 EDIT 文档拿到），**性价比为负，止损**。

## 六、对未解项的战术意义

1. **资金定位改为精确值对拍**：请玩家提供游戏内界面显示的账户余额/转会预算/
   工资预算数值，将数值 ×100 后在解码数据中全量检索；命中且相邻位置存在
   其余金额字段即可钉死。
2. 盲扫（量级窗口法）已验证噪声过大（单样本 4.7 万簇），不再采用。
3. 球员年薪/市值若出现在用户球员相关区域，按 €100 单位检索。
